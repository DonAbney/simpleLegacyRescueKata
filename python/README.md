# Python simpleLegacyRescueKata

This project runs in both Python 3.x and 2.7

# How to run tests:

from test directory:
  python -m unittest someclasstest

from main directory:
python -m unittest discover
